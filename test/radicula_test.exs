defmodule RadiculaTest do
  use ExUnit.Case

  alias Radicula.{Filtrator, GeneratorSuperviser}


  doctest Radicula

  test "greets the world" do
    assert Radicula.hello() == :world
  end

  test "integration test" do
    
  end
  
end
