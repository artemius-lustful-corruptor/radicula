--- 
networks: 
  app-tier: 
    driver: bridge
services: 
  radicula: 
    build: "."
    networks: 
      - app-tier
  redis: 
    environment: 
      - ALLOW_EMPTY_PASSWORD=yes
    image: "bitnami/redis:latest"
    networks: 
      - app-tier
version: "3.9"
